[workspace]
members  = ["crates/nixman-core", "crates/nixman-cli", "crates/nixman-mcp"]
resolver = "2"

[profile.release]
codegen-units = 1
lto           = true
opt-level     = "s"
panic         = "abort"
strip         = true
